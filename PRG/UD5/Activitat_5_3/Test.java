package Activitat_5_3;

public class Test {
    public static void main(String[] args) {

        // Creamos una instancia para la clase 'Hores', especificamos los valores.
        Hora h = new Hora(20, 00, 00);
        
        h.incrementar_segundo(30); // Llamamos al método 'incrementar_segundo' y introducimos los segundos que queremos que se incrementen. 
    }
}
